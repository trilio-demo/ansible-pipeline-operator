apiVersion: triliovault.trilio.io/v1
kind: Backup
metadata:
  name: {{ trilio_kubernetes_backup_name }}-{{ item }}-{{ trilio_kubernetes_uniq_id }}
  namespace: {{ item }}
spec:
  type: {{ trilio_kubernetes_backup_type }}
  backupPlan:
    kind: "BackupPlan"
    name: {{ trilio_kubernetes_backupplan_name }}-{{ item }}
    namespace: {{ item }}
